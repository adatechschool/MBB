# common/apps.py

"""Django app configuration for the common package."""

from django.apps import AppConfig


class CommonConfig(AppConfig):
    """Django app configuration for the common package."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "common"
