# likes\service\apps.py

"""Django app configuration for the likes service."""

from django.apps import AppConfig


class LikesServiceConfig(AppConfig):
    """Django app configuration for the likes service."""

    name = "likes.service"
    label = "likes_service"
