# requirements.txt

# Django – version 5.2 (ou compatible)
Django>=5.2,<6.0

# Django REST Framework pour la création d'API
djangorestframework>=3.12

# Gestion des variables d'environnement
python-dotenv>=0.15

# Génération de données factices pour les commandes de population
Faker>=13.0

# Outils de test
pytest>=6.2
pytest-django>=4.4

# Connecteur PostgreSQL (adapté pour le paramétrage django.db.backends.postgresql)
psycopg2-binary>=2.9


# django-cors-headers pour gérer les politiques CORS
django-cors-headers>=3.13

# SQLAlchemy Code Generator pour convertir des bases PostgreSQL en classes Python
sqlacodegen

# PyJWT pour gérer les tokens JWT
PyJWT